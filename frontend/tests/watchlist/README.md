# Watchlist Tests

Pure Watchlist state/layout tests live here. Keep section rename/delete/reorder behavior in
`src/store/watchlistLayout.ts` so UI, persistence, and future backend sync can share the same index
rules.

Run:

```bash
npm run test:watchlist
```
