//@version=6
indicator("Moving Average (EMA)", overlay = true)
length = input.int(21, "Length", minval = 1, maxval = 500)
lineColor = input.color(#ff6d00, "Color")
plot(ta.ema(close, length), "ema", color = lineColor, linewidth = 2)
